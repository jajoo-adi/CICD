name: Simple CI Pipeline Practical

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Show files
      run: ls

    - name: Run Test
      run: echo "Pipeline executed successfully"

